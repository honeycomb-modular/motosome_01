# motosome bench — Python deps (install on the Beckhoff)
#   pip install --user --break-system-packages -r requirements.txt
PySide6>=6.5      # GUI
pysoem>=1.1       # EtherCAT master (real drive backend; not needed for the simulator)
